import React from 'react';
import { View, Image, Text, Button } from 'react-native';

import { styles } from './styles';

const NotFound = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>🏗️ Sección en construcción 👷</Text>
        </View>
    );
}

export default NotFound;